package <missing>;

public class GlobalMembers
{
	public static int Main()
	{
		int sum;
		int a;
		int b;
		int c;
		int d;
		int e;
		int f;
		while ((a = Integer.parseInt(ConsoleInput.readToWhiteSpace(true))).length() > 0 && (b = Integer.parseInt(ConsoleInput.readToWhiteSpace(true))).length() > 0 && (c = Integer.parseInt(ConsoleInput.readToWhiteSpace(true))).length() > 0 && (d = Integer.parseInt(ConsoleInput.readToWhiteSpace(true))).length() > 0 && (e = Integer.parseInt(ConsoleInput.readToWhiteSpace(true))).length() > 0 && (f != 0 && (a != 0 || b != 0 || c != 0 || d != 0 || e != 0 || f != 0 = Boolean.parseBoolean(ConsoleInput.readToWhiteSpace(true))).length() > 0)
		{
			sum = (d + 12 - a) * 3600 + (e - b) * 60 + f - c;
			System.out.print(sum);
			System.out.print("\n");
		}
		return 0;
	}

}

