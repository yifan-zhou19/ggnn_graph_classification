//******************************
//???:2.cpp
//??: ??? 1300012754
//??:2013.12.06
//??:???????? (???????????????????
//******************************

int m; // m:?????????,n:?????
int n;
int[][] map = new int[10][10]; // ???
int Breed = new int(int, int, int); // ????

int main()
{
//C++ TO JAVA CONVERTER TODO TASK: The memory management function 'memset' has no equivalent in Java:
	memset(map, 0, (Integer.SIZE / Byte.SIZE)); // ?????
	m = Integer.parseInt(ConsoleInput.readToWhiteSpace(true));
	n = Integer.parseInt(ConsoleInput.readToWhiteSpace(true));
	// ??1???9?
	for (int i = 1; i <= 9; i++)
	{
		System.out.print(Breed(i, 1, n));
		for (int j = 2; j <= 9; j++)
		{
			System.out.print(" ");
			System.out.print(Breed(i, j, n));
		}
		System.out.print("\n");
	}
	return 0;
}

// ????,x,y????,p??????
int Breed(int x, int y, int p)
{
	if (p == 0) // ????????0?
	{
		if (x == 5 && y == 5) // ??????,????m
		{
			return m;
		}
		else
		{
			return 0; // ???????0
		}
	}
	else
	{
		return Breed(x + 1, y, p - 1) + Breed(x + 1, y + 1, p - 1) + Breed(x, y + 1, p - 1) + Breed(x - 1, y + 1, p - 1) + Breed(x - 1, y, p - 1) + Breed(x - 1, y - 1, p - 1) + Breed(x, y - 1, p - 1) + Breed(x + 1, y - 1, p - 1) + 2 * Breed(x, y, p - 1); // ???????, ???????????????????????????2???????????
	}
}


