import java.util.*;

package <missing>;

public class GlobalMembers
{
	/*??
	 ???????????s??????s1?
	 
	 ?????s1??????????s???????ASCII????????ASCII???????????? ?????s???????ASCII????????ASCII????????????????????????s???????????????????????????s???????ASCII??s???????ASCII??
	 ????
	 ???????????????????2?????100
	 ????
	 ????????????
	 ????
	 1234
	 ????
	 cege
	*/
	public static int Main()
	{
		String string1 = new String(new char[120]);
		String string2 = new String(new char[120]);
//C++ TO JAVA CONVERTER TODO TASK: The memory management function 'memset' has no equivalent in Java:
		memset(string1,'\0',(Character.SIZE / Byte.SIZE));
//C++ TO JAVA CONVERTER TODO TASK: The memory management function 'memset' has no equivalent in Java:
		memset(string2,'\0',(Character.SIZE / Byte.SIZE));
		String p = null;
		String q = null;
		q = string1;
		p = string2;
		string1 = new Scanner(System.in).nextLine();
		int length = string1.length();
		for (int i = 0;i < length;i++)
		{
			if (i == (length - 1))
			{
				*(p.Substring(i)) = *(q.Substring(i)) + q;
			}
			else
			{
				*(p.Substring(i)) = *(q.Substring(i)) + (*(q.Substring(i) + 1));
			}
		}
		*(p.Substring(length)) = '\0';
		System.out.print(string2);
		System.out.print("\n");
		return 0;
	}
}
