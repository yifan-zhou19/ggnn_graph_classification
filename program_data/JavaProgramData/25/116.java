package <missing>;

public class GlobalMembers
{
	/**
	 *@ file    1000012834_002homework.cpp
	 *@ author  ???
	 *@ date    2010-12-15
	 *@ description 
	 *          ?????????2?N??
	 */
	public static int Main()
	{
		int j; // ??????i,j?????len,????n
		int i;
		int len;
		int n;
		int[] involution = new int[5000]; // ?????????????
//C++ TO JAVA CONVERTER TODO TASK: The memory management function 'memset' has no equivalent in Java:
		memset(involution, 0, (Integer.SIZE / Byte.SIZE)); // ?????
		n = Integer.parseInt(ConsoleInput.readToWhiteSpace(true));
		involution[0] = 1; // ???1??????1
		len = 1; // ???????1
		for (j = 1; j <= n; j++) // ????n?
		{
			for (i = 0; i < len; i++) // ???????*2???
			{
				involution[i] *= 2;
			}

			// ??????????????9??????????
			for (i = 0; i < len; i++)
			{
				if (involution[i] >= 10)
				{
					involution[i + 1] += involution[i] / 10;
					involution[i] %= 10;
				}
			}

			// ???????????????????????????????+1
			if (involution[len] != 0)
			{
				len++;
			}
		}
		for (j = len - 1; j >= 0; j--) // ???????
		{
			System.out.print(involution[j]);
		}
		System.out.print("\n");

		return 0;
	}
}

