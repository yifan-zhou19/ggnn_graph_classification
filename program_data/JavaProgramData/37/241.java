import java.util.*;

package <missing>;

public class GlobalMembers
{
	//****************************************************** 
	/* ? ? ?   ?????????????
	/* ? ?      ????                           
	/* ? ? ? ??2010 ? 11  ?  17  ?     
	/* ? ? ? ??????????????
	/* ? ?      ?1000012752        
	*******************************************************/ 
	public static int Main()
	{
		int t;
		int s;
		int q;
		int i;
		int j;
		int k;
		int len;
		String str = new String(new char[100000]);
		t = Integer.parseInt(ConsoleInput.readToWhiteSpace(true));
		System.in.read(); //??????????cingetline??

		for (i = 1 ; i <= t ; i++)
		{
			q = 0; //q??????????????
			str = new Scanner(System.in).nextLine();
			len = str.length(); //???
			for (j = 0 ; j < len ; j++)
			{
				s = 1; //???????s????????????j???????0
				if (str.charAt(j) >= 'a' && str.charAt(j) <= 'z')
				{
					for (k = j + 1 ; k < len ; k++) //?j???????
					{
						if (str.charAt(k) == str.charAt(j)) //?????j??????????????????????????a-z
						{
							s = 0; //??????0???????
							str = str.substring(0, k);
						}
					}
					if (s == 1) //??????????
					{
						System.out.print(str.charAt(j));
						System.out.print("\n");
						q = 1;
						break;
					}
				}
			}
			if (q == 0) //??????no
			{
			System.out.print("no");
			System.out.print("\n");
			}
		}
		return 0;
	}




}

