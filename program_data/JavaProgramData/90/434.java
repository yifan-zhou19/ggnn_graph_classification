package <missing>;

public class GlobalMembers
{
	/**
	 *@ file    1000012834_004homework.cpp
	 *@ author  ???
	 *@ date    2010-12-7
	 *@ description 
	 *          ????????????
	 */

	// combine??????????????
	public static int combine(int m1_apple, int n1_plate)
	{
		int total1; // ????????????????????
		int total2;
		int t;
		if (n1_plate == 1 || m1_apple == 0) // ??????????????????1????
		{
			return 1;
		}
		else
		{
			if (m1_apple < n1_plate) // ?????????????????????????????
			{
				return combine(m1_apple, m1_apple); // ????
			}
			else
			{ // ????????????
				total1 = combine(m1_apple, n1_plate - 1);
				total2 = combine(m1_apple - n1_plate, n1_plate);
				t = total1 + total2; // ?????????
				return t; // ?????
			}
		}
	}
	public static int Main()
	{

		// ????????t,????i,????m_apple,????n_plate????????total
		int t;
		int i;
		int m_apple;
		int n_plate;
		int total;
		t = Integer.parseInt(ConsoleInput.readToWhiteSpace(true));
		for (i = 1; i <= t; i++) // ?????????
		{
			m_apple = Integer.parseInt(ConsoleInput.readToWhiteSpace(true));
			n_plate = Integer.parseInt(ConsoleInput.readToWhiteSpace(true));
			total = combine(m_apple, n_plate); // ???????????
			System.out.print(total);
			System.out.print("\n");
		}

		return 0;
	}
}

