package <missing>;

public class GlobalMembers
{
	/*
	  Name:???? 
	  Author: ??? 
	  Date: 26/12/12 17:01
	*/
	public static int Main()
	{
		String string = new String(new char[10000]);
		String subString = new String(new char[300]);
		String replacement = new String(new char[1000]);
		String = ConsoleInput.readToWhiteSpace(true);
		subString = ConsoleInput.readToWhiteSpace(true).charAt(0);
		replacement = ConsoleInput.readToWhiteSpace(true).charAt(0);
//C++ TO JAVA CONVERTER TODO TASK: Pointer arithmetic is detected on this variable, so pointers on this variable are left unchanged:
		char * p;
//C++ TO JAVA CONVERTER TODO TASK: Pointer arithmetic is detected on this variable, so pointers on this variable are left unchanged:
		char * q;
		q = replacement;
		p = tangible.StringFunctions.strStr(String, subString); //???????
		if (p != null) //???????
		{
			while (*q != null)
			{
				*p++= *q++;
			}
		}
		System.out.print(String);
		System.out.print("\n");
		return 0;
	}

}

