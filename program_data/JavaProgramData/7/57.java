package <missing>;

public class GlobalMembers
{
	public static int Main()
	{
		String string = new String(new char[256]);
		String substring = new String(new char[256]);
		String replacement = new String(new char[256]);
		int x;
		String = new Scanner(System.in).nextLine();
		substring = new Scanner(System.in).nextLine();
		replacement = new Scanner(System.in).nextLine();
		x = tangible.StringFunctions.strStr(String,substring) - string.charAt(0);
		if (tangible.StringFunctions.strChr(String,substring.charAt(0)) != 0)
		{
//C++ TO JAVA CONVERTER TODO TASK: The memory management function 'memmove' has no equivalent in Java:
			memmove(string.Substring(x),replacement,replacement.length());
		}
		System.out.printf("%s",String);
		return 0;
	}


}

