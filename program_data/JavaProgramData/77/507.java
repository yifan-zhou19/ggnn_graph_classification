import java.util.*;

package <missing>;

public class GlobalMembers
{
	// *******************
	// ** ????.cpp  **
	// ** ID 1000012895 **
	// ** ???        **
	// ** 2010 12 259   **
	// *******************
	public static char sign1; // ????????????????
	public static char sign2;
	public static void go_out(String line, int len) // ??go_out
	{
		int temp;
		for (int i = 0; i < len - 1; i++) // ????
		{
			if (line[i].equals(sign1)) // ????????
			{
				temp = i + 1;
				while (line[temp].equals('@')) // ??????????????@?????????temp????????@?
				{
					temp++;
				}
				if (line[temp].equals(sign2)) // ????????????????????????????@?????
				{
					System.out.print(i);
					System.out.print(' ');
					System.out.print(temp);
					System.out.print("\n");
					line[temp] = line[i] = '@';
					return; // ?????
				} // ???????????????????????????
			}
		}
		return;
	}

	public static int Main()
	{
		String line = new String(new char[1000]);
		int len;
		line = new Scanner(System.in).nextLine(); // ???????
		sign1 = line.charAt(0); // ???????????????sign1?
		int temp = 1;
		while (line.charAt(temp) == sign1)
		{
			temp++;
		}
		sign2 = line.charAt(temp); // ???????sign1?????????????????sign2
		len = (Character.SIZE / Byte.SIZE);
		while (line.charAt(0) == sign1) // ???????????????????
		{
			go_out(line, len);
		}
		return 0;
	}
}
