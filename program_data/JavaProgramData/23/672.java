package <missing>;

public class GlobalMembers
{
	public static int Main()
	{
	  char[][] ch = new char[50][100];
	  int n = 0;
	  for ((ch[n]; ch[n][0] != '\n' && ch[n][0] != '\0'; = Boolean.parseBoolean(ConsoleInput.readToWhiteSpace(true))).length() > 0)
	  {
		n++;
		ch[n] = ConsoleInput.readToWhiteSpace(true).charAt(0);
	  }

	  for (n--; n >= 0; n--)
	  {
		System.out.print(ch[n]);
		if (n > 0)
		{
			System.out.print(' ');
		}
	  }
	  return 0;
	}

}

