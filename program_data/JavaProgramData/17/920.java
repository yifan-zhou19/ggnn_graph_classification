import java.util.*;

package <missing>;

public class GlobalMembers
{
	public static String a = new String(new char[150]);
	public static int find(int x,int y) //x???????y??????
	{
		int b; //b???????????????????????
		if (a.charAt(x) == '\0')
		{
			return -1;
		}
		if (a.charAt(x) == '(')
		{
		  b = find(x + 1, y + 1); //????????????????
		  a = tangible.StringFunctions.changeCharacter(a, x, (b == -1)?'$':' '); //??????$?????????
		  return (b == -1)?-1:find(b + 1, y); //?????????????????
		}
		if (a.charAt(x) == ')')
		{
		  a = tangible.StringFunctions.changeCharacter(a, x, (y == 0)?'?':' '); //??????????????????
		  return (y == 0)?find(x + 1, y):x; //?????????????????????
		}
		else //??????????????????
		{
		  a = tangible.StringFunctions.changeCharacter(a, x, ' ');
		  return find(x + 1, y);
		}
	}
	public static int Main()
	{
		int n = 1000;
		for (int i = 1;i < n;i++)
		{
		  a = new Scanner(System.in).nextLine();
		  if (a.charAt(0) == '\0')
		  {
		  break;
		  }
		  else
		  {
			System.out.print(a);
			System.out.print("\n");
			find(0, 0); //????
			System.out.print(a);
			System.out.print("\n");
		  }
		}
		return 0;
	}

}

