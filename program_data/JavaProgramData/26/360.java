import java.util.*;

package <missing>;

public class GlobalMembers
{
	/**
	 * ???????????.cpp
	 * ?????????????
	 * ??????
	 * ???2010.12.10
	 */

	public static int Main()
	{
		String sen = new String(new char[101]); //sen??????
//C++ TO JAVA CONVERTER TODO TASK: Pointer arithmetic is detected on this variable, so pointers on this variable are left unchanged:
		char * p = null;
//C++ TO JAVA CONVERTER TODO TASK: Pointer arithmetic is detected on this variable, so pointers on this variable are left unchanged:
		char * q = null;
		int i; //num1???????????????,len????????
		int num1;
		int len;
		len = 0;
		sen = new Scanner(System.in).nextLine(); //??????
		p = sen; //??p??????????
		for (i = 1; * ++p != '\0'; i++)
		{
			if (*p == ' ') //???????
			{
				if (*(p - 1) != ' ') //?????????????
				{
					num1 = i;
				}
				if (*(p + 1) != ' ') //???????????
				{
					for (q = sen.Substring(num1); * (q + i - num1) != '\0'; q++) //?????????????
					{
						*q = (q + i - num1);
					}
					*q = (q + i - num1);
					p -= i - num1; //p????????
					len += i - num1; //??len
					i -= i - num1;
				}
			}
		}
		for (p = sen; p < i + sen; p++) //??????????????
		{
			System.out.print(p);
		}
		System.out.print("\n");

		return 0;
	}
}
