package <missing>;

public class GlobalMembers
{
	/*??
	 ???????????????????????????????????????????????????
	 ?????80???????????????????????????????????????????
	 ????
	 ????????n?????????????. ???n??????????????????????????????????????40?????
	 ????
	 ??????????????????80????????????????????????????*/
	public static int Main()
	{
		int n = 0;
		int sum = 0;
		int length = 0;
		String p = null;
		String istring = new String(new char[30]);
		n = Integer.parseInt(ConsoleInput.readToWhiteSpace(true));
		p = istring;
//C++ TO JAVA CONVERTER TODO TASK: The memory management function 'memset' has no equivalent in Java:
		memset(istring,'\0',(Character.SIZE / Byte.SIZE));
		istring = ConsoleInput.readToWhiteSpace(true).charAt(0);
		System.out.print(istring);
		sum = istring.length();
		for (int i = 0;i < n - 1;i++)
		{

//C++ TO JAVA CONVERTER TODO TASK: The memory management function 'memset' has no equivalent in Java:
			memset(istring,'\0',(Character.SIZE / Byte.SIZE));
			istring = ConsoleInput.readToWhiteSpace(true).charAt(0);
			length = istring.length();
			if (sum + length + 1 > 80)
			{
				System.out.print("\n");
				sum = length;
				System.out.print(istring);
			}
			else
			{
				sum = sum + length + 1;
				System.out.print(" ");
				System.out.print(istring);

			}
		}
		return 0;
	}

}

