import java.util.*;

package <missing>;

public class GlobalMembers
{
	public static int Main()
	{
		String sen = new String(new char[31]); //sen?????????*p???
//C++ TO JAVA CONVERTER TODO TASK: Pointer arithmetic is detected on this variable, so pointers on this variable are left unchanged:
		char * p = null;
		int flag = 0; //flag????len???????
		int len;
		sen = new Scanner(System.in).nextLine();
		len = sen.length(); //????????
		for (p = sen ; p < sen.Substring(len) ; p++) //???????????
		{
			if (*p >= '0' && *p <= '9') //????????????flag??1
			{
				System.out.print(p);
				flag = 1;
			}
			if ((*p < '0' || *p > '9') && flag == 1) //??????????????????????????flag??0
			{
				System.out.print("\n");
				flag = 0;
			}
		}
		return 0;
	}
}
