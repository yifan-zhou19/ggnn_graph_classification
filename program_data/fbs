input=CppProgramData
in=${1:-cpp_protobuf_format}
n=${2:-3}

mkdir -p $in
chmod -R a+w $in
for ((i=1;i<=$n;i++)); do
    if [ ! -f $in/$i.fbs ]; then
	fast -v > $in/fast.version
	fast -S -G $input/$i $in/$i.fbs
    fi
done
